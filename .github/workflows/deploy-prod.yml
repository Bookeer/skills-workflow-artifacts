name: Deploy Prod

on:
  workflow_run:
    workflows: ["Build and Deploy"]
    types:
      - completed

permissions: 
  contents: read
  actions: read

jobs:
  prod:
    name: Deploy Prod
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'succes'
    environment: prod
    steps:
      - uses: actions/download-artifact@v8
        with: 
          name: octomatch
          path: website
          run-id: ${{ github.event.workflow_run.id}}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to Production
      run: |
        echo: "Downloaded artifacts contents:"
        tree wesite
        echo  "Demo deply step: replace with your real deploy command"
      
    
